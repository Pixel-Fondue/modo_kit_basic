<?xml version="1.0" encoding="UTF-8"?>
<configuration kit="my_modo_kit" version="0.0.0">

  <!-- Import the kit itself. -->
  <import></import>
  <!-- Import the python libraries. -->
  <import>libs</import>
  <!-- Import the config files. -->
  <import>configs</import>

</configuration>
