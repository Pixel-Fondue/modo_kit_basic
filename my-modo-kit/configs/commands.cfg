<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <atom type="CommandHelp">
    <!-- note the command's name in here; my.command -->
    <hash type="Command" key="mmk.name@en_US">
      <atom type="UserName">Modo Kit Name</atom>
      <atom type="ButtonName">Modo Kit Name</atom>
      <atom type="Tooltip">Prints out a greeting for the given name.</atom>
      <atom type="Desc">Example command that prints out a greeting for the given name.</atom>
      <!-- An example of how my command would be called - shown in the command list. -->
      <atom type="Example">
mmk.name name:YourName
lx.command("mmk.name", name='YourName')</atom>

      <hash type="Argument" key="name">
        <atom type="UserName">Name</atom>
        <atom type="Desc">The name of the user to greet.</atom>
      </hash>

    </hash>
  </atom>
</configuration>
